# Data Manifests

Place small, version-controlled manifests here.

Suggested files:

- `samples.csv`: one row per growth/sample id
- `pairs.csv`: AFM/RHEED pairing metadata
- `splits.csv`: train/validation/test split assignments

Do not store raw AFM/RHEED measurements or large processed arrays in this folder.
