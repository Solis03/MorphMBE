# Unseen Inference

Run:

```bash
python 13_UNSEEN_INFERENCE/predict_unseen_batch.py --bundle-root <freeze_root> --manifest <unseen_manifest.csv> --output-root <prospective_run_dir> --freeze-id RHEED_AFM_PAPER_FREEZE_V1_20260716_183134
```

If keyframe/ROI are missing, stop and use the manual review workflow before prediction.

