# Rq definition

Rq is computed from finite physical AFM height pixels in nm: z_i' = z_i - mean(z), Rq = sqrt((1/N) sum_i z_i'^2). Rq is never computed from rendered PNGs. Scan-level Rq and sample-level Rq are distinct; growth group is the statistical unit.
