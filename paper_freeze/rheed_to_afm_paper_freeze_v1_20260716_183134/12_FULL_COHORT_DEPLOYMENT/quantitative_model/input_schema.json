{
  "input": "RHEED sample metadata and DINO embedding",
  "required_fields": [
    "sample_id",
    "video_path",
    "keyframe_index",
    "roi_x",
    "roi_y",
    "roi_width",
    "roi_height"
  ]
}
