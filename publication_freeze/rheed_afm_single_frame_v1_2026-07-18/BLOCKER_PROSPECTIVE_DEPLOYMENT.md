# Blocker: Prospective Unseen Deployment Is Not Frozen As Production

The strict OOF single-frame result is frozen here, but a canonical end-to-end `predict_unseen.py` is intentionally not packaged. Historical smoke/batch scripts include deterministic placeholder embedding vectors or Rq-nearest shortcuts in places, so packaging them as production would overstate the deployed method.

Frozen here: strict OOF RHEED-to-Rq predictions, full-cohort quantitative model artifacts, strict A3 retrospective visual retrieval evidence, schemas, and provenance.

Required before prospective deployment: validate a single canonical raw-RHEED-to-DINO-to-Rq-to-A3 CLI without placeholder vectors.
